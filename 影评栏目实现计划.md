# 影评栏目实现计划

> 规格见 [影评栏目设计.md](影评栏目设计.md)。这份只管**怎么做、按什么顺序做**。
> 做完这份计划可以删掉，规格里的规则要并进 [CLAUDE.md](CLAUDE.md)。

**目标：** 网站分成 照片／影片／影评 三大类别，并加入 62 篇影评和 62 张海报。

**做法：** 沿用现有的单文件生成器。`kind` 字段扩展出第三个值 `"notes"`；
影评是**一个 toml、一个页面**，不是 62 个文件、124 个页面。海报走**现有的图片流水线**，
只换一套宽度。零新增依赖，零 JavaScript。

**工具链：** Python 3.11+（`tomllib`）、ImageMagick、cwebp、playwright-core（仅验证）。
新增一个网络依赖：**TMDB API**（仅在拉海报时用，构建本身不联网）。

---

## 全局约束

这些是全程都生效的，每个任务都隐含包含：

- **网址一旦发布就不能改。** 新增 `/film-notes/`、`/zh/film-notes/` 和 62 个锚点，定了不能动。
- **中文他写、英文我转。** 中文版是原稿，英文从中文转，**中文绝不从英文回译**。
- **关掉 JS 也要能看。** 所有 `opacity: 0` 必须挂在 `.js-reveal` 下面，由 `gallery.js` 亲手加。
- **`sizes` 里不能写百分比。** 只能用合法的 CSS 长度，否则整条属性被浏览器丢掉、回退成 `100vw`。
- **尺寸写在两个地方必须同步：** `build.py` 的常量 ↔ `style.css` 的对应值。
- **绝对不要同时跑两个 `build.py`。** 会让 AVIF 保险丝量到写了一半的文件，误删好图。
- **不替他 push。** 每个任务末尾给命令，他自己在终端跑。
- **改了中文必须：** `build.py` → `subset_font.py` → `build.py`，顺序不能变。

---

## 文件清单

| 文件 | 职责 | 动作 |
|---|---|---|
| `site/content/film-notes.toml` | 62 篇影评的全部文字（中英） | 新建 |
| `site/content/posters.toml` | slug → TMDB 编号／海报路径，**生成后由他核对** | 新建（生成） |
| `site/tools/fetch_posters.py` | 按片名搜 TMDB、下载海报 | 新建 |
| `site/tools/check_notes.py` | 校验 62 条内容完整性（本项目的"测试" ) | 新建 |
| `site/templates/film-notes.html` | 影评页模板 | 新建 |
| `site/build.py` | 加 `build_notes` / `render_notes` / `catnav`，首页分区 | 改 |
| `site/templates/index.html` | 分类导航 + 三个区 | 改 |
| `site/static/style.css` | 导航、区标题、海报墙、影评条目 | 改 |
| `素材/海报/*.jpg` | 海报原图，**已被 `.gitignore` 挡住，不进仓库** | 新建 |
| `CLAUDE.md` / `README.md` / `容量规划.md` | 文档 | 改 |
| scratchpad 的 `verify.js` | 验证脚本 | 改 |

---

## 任务 1：TMDB 对照表（含他的核对关卡）

**为什么排第一：** 片名错了海报就错了。第六组写 alt 文字踩过这个坑——
文件和内容的对应必须**先**确认，不能事后补。

**文件：** 新建 `site/tools/fetch_posters.py`、生成 `site/content/posters.toml`

**Interfaces —— 后面的任务依赖这些：**
- 产出 `素材/海报/<slug>.jpg`，`<slug>` = 英文片名 slugify 后的结果
- `posters.toml` 结构：`[[poster]]` 每条含 `slug` / `tmdb` / `title` / `year` / `poster_path`

- [ ] **步骤 1：他去拿一个免费的 TMDB API key**

  <https://www.themoviedb.org/settings/api> → 注册 → API Read Access Token。
  **这是他的账号，我不代注册。** 拿到后：

  ```bash
  export TMDB_API_KEY='贴在这里'
  ```

- [ ] **步骤 2：写 `fetch_posters.py`，先只做 `--resolve`**

  读 `film-notes.toml` 里每条的 `title` 和 `year`，调 TMDB 的 `/search/movie`，
  取第一个结果，写出 `posters.toml`，并把对照表打到屏幕上。

  ```python
  #!/usr/bin/env python3
  """按片名到 TMDB 搜海报。分两步：--resolve 出对照表，他核对之后再 --download。

  ⚠️ 两步分开是故意的。片名搜错了海报就错了，而错了很难一眼看出来
     ——62 张小图里混进一张同名重拍的，谁都不会发现。
  """
  import os, sys, json, time, tomllib, urllib.request, urllib.parse
  from pathlib import Path

  SITE = Path(__file__).resolve().parent.parent
  NOTES = SITE / "content" / "film-notes.toml"
  LOCK = SITE / "content" / "posters.toml"
  DEST = SITE.parent / "素材" / "海报"
  IMG = "https://image.tmdb.org/t/p/w500"

  def api(path, **params):
      key = os.environ.get("TMDB_API_KEY")
      if not key:
          sys.exit("没有 TMDB_API_KEY。见 影评栏目实现计划.md 任务 1 步骤 1。")
      url = f"https://api.themoviedb.org/3{path}?" + urllib.parse.urlencode(params)
      req = urllib.request.Request(url, headers={
          "Authorization": f"Bearer {key}", "accept": "application/json"})
      # 他的网络到境外不稳（CLAUDE.md 第七节），重试三次再放弃
      for i in range(3):
          try:
              with urllib.request.urlopen(req, timeout=20) as r:
                  return json.load(r)
          except Exception as e:
              if i == 2:
                  raise
              time.sleep(3)
  ```

  `--resolve` 主体：对每条 `entry` 搜索，**把候选数量和年份差一并打出来**——
  只有一个结果且年份对得上的标 `✓`，其余标 `?` 要他重点看。

- [ ] **步骤 3：跑 resolve，把对照表交给他核对**

  ```bash
  cd ~/Desktop/website && python3 site/tools/fetch_posters.py --resolve
  ```

  预期输出形如：

  ```
  ✓ 致命魔术              The Prestige                    2006  tmdb 1124
  ✓ 死亡诗社              Dead Poets Society              1989  tmdb 207
  ? 情书                  Love Letter                     1995  tmdb 25656   （3 个同名结果）
  ? 大话西游之月光宝盒     A Chinese Odyssey Part One      1995  tmdb 11385   （年份差 1）
  ```

  **这一步必须停下来等他确认。** 有疑问的逐条问，不猜。

- [ ] **步骤 4：确认后加 `--download`，把 62 张拉下来**

  ```bash
  cd ~/Desktop/website && python3 site/tools/fetch_posters.py --download
  ```

  写到 `素材/海报/<slug>.jpg`。**下载前脚本要打印总数和预估体积**，
  并且已经下过的跳过（可以断点续传——他的网络会断）。

- [ ] **步骤 5：提交（海报原图不会进去，`.gitignore` 挡着）**

  ```bash
  cd ~/Desktop/website && git add site/tools/fetch_posters.py site/content/posters.toml && git commit -m "Add TMDB poster resolver and the reviewed film table"
  ```

---

## 任务 2a：62 篇中文入 toml

**文件：** 新建 `site/content/film-notes.toml`、新建 `site/tools/check_notes.py`

**Interfaces —— 产出的数据结构，任务 3 靠它渲染：**
```toml
kind = "notes"; slug = "film-notes"; title = "Film Notes"; order = 100
statement = """…"""            # 英文开场白
[zh]
title = "影评"; statement = """…"""

[[entry]]
year = 2006
title = "The Prestige"          # slug 由它生成
text  = """…"""                 # 英文正文（任务 2b 填）
note_kind = "class"             # 可选，只有三篇课堂笔记写
  [entry.zh]
  title = "致命魔术"
  text  = """…"""
```

- [ ] **步骤 1：先写校验脚本（这是本项目意义上的"先写测试"）**

  `site/tools/check_notes.py`，跟 `check_font.py` 一个路数——不联网、退出码表示成败：

  ```python
  #!/usr/bin/env python3
  """校验 film-notes.toml。跑在构建之前，把内容问题挡在生成之前。"""
  import sys, re, tomllib
  from pathlib import Path

  NOTES = Path(__file__).resolve().parent.parent / "content" / "film-notes.toml"

  def slugify(t):
      return re.sub(r"[^a-z0-9]+", "-", t.lower()).strip("-")

  def main():
      cfg = tomllib.loads(NOTES.read_text(encoding="utf-8"))
      entries = cfg.get("entry", [])
      bad = []
      if len(entries) != 62:
          bad.append(f"应该有 62 条，实际 {len(entries)} 条")
      seen = {}
      for i, e in enumerate(entries, 1):
          tag = f"第 {i} 条"
          for key in ("title", "year", "text"):
              if not e.get(key):
                  bad.append(f"{tag} 缺 {key}")
          zh = e.get("zh", {})
          for key in ("title", "text"):
              if not zh.get(key):
                  bad.append(f"{tag}（{e.get('title','?')}）缺 zh.{key}")
          s = slugify(e.get("title", ""))
          if s in seen:
              bad.append(f"slug 撞车：{s} —— 第 {seen[s]} 条和{tag}")
          seen[s] = i
          for key, val in (("text", e.get("text","")), ("zh.text", zh.get("text",""))):
              if "【" in str(val):
                  bad.append(f"{tag} 的 {key} 还是占位符")
      if bad:
          print("✗ film-notes.toml 有问题：")
          for b in bad:
              print(f"    {b}")
          sys.exit(1)
      print(f"✓ {len(entries)} 条全部完整，slug 无撞车")

  if __name__ == "__main__":
      main()
  ```

- [ ] **步骤 2：跑它，确认它失败**

  ```bash
  cd ~/Desktop/website && python3 site/tools/check_notes.py
  ```
  预期：`FileNotFoundError`（film-notes.toml 还不存在）。

- [ ] **步骤 3：写入 62 条的中文，含规格第六节的六项修订**

  素材：`/private/tmp/.../scratchpad/reviews.txt`（已从 docx 抽出）。
  **顺序照抄原文档，不重排**——那个顺序是观影顺序，是这一页最值钱的东西。

  逐条要做的：

  | 改动 | 具体 |
  |---|---|
  | 《花样年华》 | 用他 2026-08-29 给的新稿替换（旧的是《欧维》的复制品） |
  | 《拯救疯人院》 | 片名改成《飞越疯人院》 |
  | 《一个叫欧维的男人的决定去死》 | 片名去掉多余的「的」；正文截在「……而妻子因为车祸而残疾，在事业上也遇到了困难。」 |
  | 《银翼杀手》 | **删掉开头那段 IMDb 机翻**，只留「元素」开始的自己写的部分 |
  | 错别字 | 「专场」→「转场」(3)、「违一镜到底」→「一镜到底」、「粉币画」→「粉笔画」、「太町市」→「太地町」、「体质」→「体制」、「详略得到」→「详略得当」、「耳目一心」→「耳目一新」、「布沙海豚」→「捕杀海豚」、「有一部神作」→「又一部神作」、「新线」→「新鲜」、「讲喧嚣」→「将喧嚣」、「拉进与观众」→「拉近与观众」、「小伟/小韦」统一 |
  | 课堂笔记 | 《大都会》《灰熊人》《银翼杀手》三条加 `note_kind = "class"` |

  **只改错字，不动语气和判断。** 他的句子是他的。

- [ ] **步骤 4：跑校验，确认通过**

  ```bash
  cd ~/Desktop/website && python3 site/tools/check_notes.py
  ```
  预期：`✗ … 缺 zh.text` 之类**不该出现**；此时英文还没写，`缺 text` 会报 62 次——
  这是对的，任务 2b 才补。**先把校验脚本里的英文检查跳过**，2b 时打开。

- [ ] **步骤 5：提交**

  ```bash
  cd ~/Desktop/website && git add site/content/film-notes.toml site/tools/check_notes.py && git commit -m "Add 62 film notes in Chinese with the six agreed corrections"
  ```

---

## 任务 2b：62 篇英文

**文件：** 改 `site/content/film-notes.toml`、改 `site/tools/check_notes.py`

- [ ] **步骤 1：把 `check_notes.py` 里对英文 `text` 的检查打开**（删掉 2a 步骤 4 加的跳过）
- [ ] **步骤 2：跑它，确认报 62 次缺 text**
- [ ] **步骤 3：逐条从中文转英文**

  - 片名用**国际通行英文名**：《致命魔术》→ The Prestige，
    《大话西游之月光宝盒》→ A Chinese Odyssey Part One: Pandora's Box
  - **短评译完仍然短**，不为了充实而加话。《黑客帝国》"一场视觉盛宴" → "A visual feast."
  - 电影术语按英文行话译：焦段 → focal length，运镜 → camera movement，
    调度 → blocking／staging，正反打 → shot-reverse-shot，升格 → slow motion，
    过肩 → over-the-shoulder，蒙太奇按类型分别处理（叙事／平行／交叉）
  - 三篇课堂笔记保留短句堆叠的形态，**不要整理成完整句子**——原样是它的诚实之处

- [ ] **步骤 4：跑校验，确认全绿**

  ```bash
  cd ~/Desktop/website && python3 site/tools/check_notes.py
  ```
  预期：`✓ 62 条全部完整，slug 无撞车`

- [ ] **步骤 5：提交**

  ```bash
  cd ~/Desktop/website && git add -A && git commit -m "Translate all 62 film notes into English"
  ```

---

## 任务 3：影评页能出来（还没有海报）

**文件：** 改 `site/build.py`、新建 `site/templates/film-notes.html`、改 `site/static/style.css`

**Interfaces —— 消费任务 2 的 toml，产出任务 4／5 依赖的东西：**
- `build_notes(cfg: dict, force: bool) -> dict`
  返回 `{"kind": "notes", "cfg": cfg, "slug": str, "entries": list, "bytes": int}`
- `render_notes(s: dict, lang: str, site: dict) -> None`
- 每条 entry 被就地加上 `entry["slug"]: str`（锚点和海报文件名都用它）

- [ ] **步骤 1：`main()` 里把 notes 从 `built` 里分出来**

  `built` 只装作品（照片＋影片），影评单独一个变量。这样「下一组」链条、
  `write_index` 的 `work_card`、构建摘要**全都不用改**：

  ```python
  built, notes = [], None
  for cfg_path in configs:
      with cfg_path.open("rb") as f:
          cfg = tomllib.load(f)
      kind = cfg.get("kind")
      # 影评不是"作品"：不进「下一组」链条，不出现在作品墙上，不参与编号
      if kind == "notes":
          notes = build_notes(cfg, args.force)
          continue
      builder = build_film if kind == "film" else build_series
      built.append(builder(cfg, args.force))
  ```

  渲染循环里加一行：

  ```python
  for lang in LANGS:
      for s in built:
          render = render_film if s["kind"] == "film" else render_series
          render(s, lang, site, next_of[s["slug"]])
      write_index(built, site, lang, notes)
      if notes:
          render_notes(notes, lang, site)
  ```

- [ ] **步骤 2：加 `build_notes`（这一版先不碰海报）**

  ```python
  def build_notes(cfg: dict, force: bool) -> dict:
      """影评：一个 toml 装 62 条，渲染成一个页面。

      跟 build_series / build_film 不同，它没有封面、没有张数，
      也不进「下一组」链条——它是另一种阅读模式。
      """
      entries = cfg.get("entry", [])
      if not entries:
          sys.exit("film-notes.toml 里一条 [[entry]] 都没有")
      seen = {}
      for e in entries:
          e["slug"] = slugify(e["title"])
          if e["slug"] in seen:
              sys.exit(f"影评 slug 撞车：{e['slug']}（{seen[e['slug']]} 和 {e['title']}）")
          seen[e["slug"]] = e["title"]
      return {"kind": "notes", "cfg": cfg, "slug": cfg["slug"],
              "entries": entries, "bytes": 0}
  ```

- [ ] **步骤 3：加 LANGS 的新词条**

  ```python
  # en
  "cat_photos": "Photographs", "cat_films": "Films", "cat_notes": "Film Notes",
  "class_notes": "Class notes", "skip_notes": "Skip to the notes",
  "notes_count": "films",
  # zh
  "cat_photos": "照片", "cat_films": "影片", "cat_notes": "影评",
  "class_notes": "课堂笔记", "skip_notes": "跳到影评",
  "notes_count": "部",
  ```

- [ ] **步骤 4：加短评阈值常量**

  ```python
  # 50 字以内的短评单独一种排法（大一号、像格言）。
  # ⚠️ 50 是拍的，渲染出来再定 —— 像照片尺寸当初比过 84/74/66/58 那样。
  #    中文按字数、英文按词数，两版各看一次。
  SHORT_NOTE_CHARS = 50
  SHORT_NOTE_WORDS = 30
  ```

- [ ] **步骤 5：写 `film-notes.html` 模板**

  ```html
  <a class="skip" href="#notes">{{ skip }}</a>
  <a class="langswitch" href="{{ switch_href }}" title="{{ switch_title }}">{{ switch_label }}</a>

  <main class="page">
    <header class="masthead">
      <div class="masthead__card">
        <p class="masthead__eyebrow">{{ eyebrow }}</p>
        <h1 class="masthead__title">{{ title }}</h1>
        <p class="masthead__meta">{{ meta }}</p>
      </div>
      {{ catnav }}
      <div class="statement">{{ statement }}</div>
    </header>

    <ol class="notes" id="notes">
  {{ entries }}
    </ol>

    <footer class="colophon">
      <p class="colophon__gear">{{ credit }}</p>
      <p class="colophon__name">{{ name }}</p>
    </footer>
  </main>
  ```

- [ ] **步骤 6：写 `render_notes` 和条目渲染**

  每条：

  ```html
  <li class="note note--short" id="the-prestige">
    <div class="note__body">
      <h2 class="note__title">The Prestige<span class="note__year"> · 2006</span></h2>
      <p>…</p>
    </div>
  </li>
  ```

  `note--short` 在正文短于阈值时加；`note--class` 在 `note_kind == "class"` 时加，
  并在标题后面挂一个 `<span class="note__tag">Class notes</span>`。

  正文用现成的 `paragraphs()`——它已经处理了分段、弯引号和占位符高亮。

- [ ] **步骤 7：加 CSS**

  沿用现有的排版尺度（`--ink` 三档灰阶、衬线正文）。**新加的灰阶必须过 WCAG AA 4.5:1**
  ——`--ink-faint: #7a7d85` 是 4.7:1，是底线，别往下调。

- [ ] **步骤 8：构建，确认两页都出来了**

  ```bash
  cd ~/Desktop/website && python3 site/build.py && ls -la docs/film-notes/ docs/zh/film-notes/
  ```
  预期：两个目录各有一个 `index.html`。

- [ ] **步骤 9：本地看一眼，特别是短评和课堂笔记那三条**

  ```bash
  cd ~/Desktop/website && python3 site/build.py --serve
  ```
  打开 <http://localhost:8000/film-notes/> 和 <http://localhost:8000/zh/film-notes/>。
  **这时定 `SHORT_NOTE_CHARS` 的最终值。**

- [ ] **步骤 10：提交**

  ```bash
  cd ~/Desktop/website && git add -A && git commit -m "Render the film-notes page in both languages"
  ```

---

## 任务 4：海报

**文件：** 改 `site/build.py`、改 `site/static/style.css`

- [ ] **步骤 1：加海报的宽度常量**

  ```python
  # ── 影评海报 ────────────────────────────────────────────────────
  # 海报是 2:3 竖幅，显示尺寸远小于照片：墙上每张约 96px 宽、条目里约 120px。
  # 400px 覆盖到 2 倍屏还有余量。**不要套用照片的 900/1600/2400**
  # ——那是给 1400px 宽的照片用的，海报用那个是白下载。
  #
  # ⚠️ 这两个数字必须跟 style.css 里 .posterwall 的 --poster-w
  #    和 .note__poster 的宽度对上。对不上不会变形，但会下错档。
  POSTER_WIDTHS = [200, 400]
  POSTER_WALL_PX = 96
  POSTER_ENTRY_PX = 120
  POSTER_SRC = SRC_ROOT / "海报"
  ```

- [ ] **步骤 2：让 `widths_for` 和 `build_images` 接受自定义宽度**

  ```python
  def widths_for(photo: Photo, widths: list[int] | None = None) -> list[int]:
      """只生成不超过原图长边的尺寸——放大没有意义。"""
      longest = max(photo.width, photo.height)
      keep = [w for w in (widths or WIDTHS) if w <= longest]
      return keep or [longest]
  ```

  `build_images(photos, slug, force, widths=None)`，内部把 `widths` 透传给
  `widths_for(photo, widths)`。**其余一行不改**——AVIF 保险丝、`.noavif` 记号、
  增量判断全部照旧生效。

- [ ] **步骤 3：`build_notes` 里加载海报**

  ```python
      posters, missing = [], []
      for e in entries:
          src = POSTER_SRC / f"{e['slug']}.jpg"
          if not src.exists():
              missing.append(e["title"]); e["poster"] = None; continue
          p = read_photo(src)
          p.alt = f"Poster for {e['title']}"
          posters.append((e, p))
      nbytes = build_images([p for _, p in posters], "film-notes", force,
                            widths=POSTER_WIDTHS)
      for e, p in posters:
          e["poster"] = p
  ```

  **缺海报不炸掉整次构建**——只记进 `missing`，在构建摘要里报警告，
  条目照常渲染（正文还在）。62 张里少 1 张不该让他连页面都构建不出来。

- [ ] **步骤 4：条目里插小海报，首页那面墙留到任务 5**

  条目里的 `sizes` 就是一个定值：`sizes="{POSTER_ENTRY_PX}px"`。
  **不能写百分比**（见全局约束）。

- [ ] **步骤 5：页脚加 TMDB 署名**

  影评页 `credit` 写 `Poster images from TMDB`。TMDB 条款要求，也是诚实的做法。

- [ ] **步骤 6：构建，量体积**

  ```bash
  cd ~/Desktop/website && python3 site/build.py && du -sh docs/img/film-notes/ && ls docs/img/film-notes/ | wc -l
  ```
  预期：62 × 2 档 × 3 格式 ≈ 372 个文件，**3–6 MB**。明显超出就停下来查。

- [ ] **步骤 7：提交**

  ```bash
  cd ~/Desktop/website && git add -A && git commit -m "Add film posters through the existing image pipeline"
  ```

---

## 任务 5：首页分三区 + 分类导航

**文件：** 改 `site/build.py`、改 `site/templates/index.html`、改 `site/static/style.css`

**排在最后做结构改动**，是因为到这一步三条导航链接才都有落点，
中间任何一次构建都不会产生断链。

- [ ] **步骤 1：写 `catnav`**

  ```python
  def catnav(lang: str, here: str) -> str:
      """三大类别导航。here ∈ {"home", "notes"}。

      ⚠️ 相对链接必须落在**本语言**那一层。中文影评页 /zh/film-notes/
         回首页是 ../ ——写成 ../../ 会掉进英文站。这条在「下一组」上踩过一次：
         查法是 new URL(href, location.href).pathname，看落点在不在同一语言下。
      """
      L = LANGS[lang]
      up = "" if here == "home" else "../"
      items = [
          ("home", f"{up}#photographs", L["cat_photos"]),
          ("home", f"{up}#films", L["cat_films"]),
          ("notes", f"{up}film-notes/" if here == "home" else "", L["cat_notes"]),
      ]
      out = []
      for owner, href, label in items:
          if owner == here and not href:
              out.append(f'<span class="catnav__item" aria-current="page">{esc(label)}</span>')
          else:
              out.append(f'<a class="catnav__item" href="{href}">{esc(label)}</a>')
      return '<nav class="catnav">' + "".join(out) + "</nav>"
  ```

- [ ] **步骤 2：`write_index` 分区**

  签名加一个参数：`write_index(series, site, lang, notes=None)`。
  `ordered` 拆成 `photo_sets` 和 `films`（这两个变量**已经存在**，现在多用一处），
  分别渲染成两个 `<ul class="works">`。

- [ ] **步骤 3：⚠️ 改 CLAUDE.md 第一节的头题那段**

  「停止滑动」不再排最前当头题——按类别分区之后不可能成立。
  **CLAUDE.md 里那段话必须同步改掉**，不然文档跟代码自相矛盾，
  下一次会话会照着旧文档改回去。

- [ ] **步骤 4：首页加海报墙**

  62 张 `POSTER_WALL_PX` 宽的小图密排，每张链到 `./film-notes/#<slug>`。
  `sizes="{POSTER_WALL_PX}px"`。

- [ ] **步骤 5：改 `index.html` 模板**

  ```html
  <ul class="works" id="photographs">{{ photo_works }}</ul>
  <ul class="works" id="films">{{ film_works }}</ul>
  <ul class="posterwall" id="film-notes">{{ posterwall }}</ul>
  ```
  加区标题。`.work--film` 现在住在只有影片的那个 `<ul>` 里，
  `flex: 0 0 100%` 和 `:not(:first-child)` 的间距**仍然正确**，不用改。

- [ ] **步骤 6：构建 + 本地查窄屏溢出**

  ```bash
  cd ~/Desktop/website && python3 site/build.py --serve
  ```
  **62 张密排最容易在窄屏撑破容器**，375px 宽下重点看。

- [ ] **步骤 7：提交**

  ```bash
  cd ~/Desktop/website && git add -A && git commit -m "Split the index into three categories with a nav"
  ```

---

## 任务 6：字体 + 文档

- [ ] **步骤 1：重裁中文字体（顺序不能变）**

  ```bash
  cd ~/Desktop/website && python3 site/build.py && python3 site/tools/subset_font.py && python3 site/build.py && python3 site/tools/check_font.py
  ```
  预期 `✓ 全部覆盖`。**报网络错误不慌**——脚本失败时不会覆盖已有字体，
  先跑 `check_font.py`，够用就照常推。

- [ ] **步骤 2：量字体涨了多少**

  ```bash
  cd ~/Desktop/website && ls -l site/static/fonts/noto-serif-sc-subset.woff2
  ```
  预计 254 KB → 约 480 KB（新增 758 个汉字）。
  **明显超过 600 KB 就停下来讨论拆子集。**

- [ ] **步骤 3：更新三份文档**

  - `CLAUDE.md`：三大类别、影评的加法、头题那段的改动（任务 5 步骤 3 已改）、
    海报的片名核对关卡
  - `README.md`：内容表加影评一行、`content/` 文件表加 `film-notes.toml` 和 `posters.toml`、
    新增 TMDB key 的说明
  - `容量规划.md`：第二节的数字重算（+海报 +字体），第八节的"什么时候要重算"加一条

- [ ] **步骤 4：提交**

  ```bash
  cd ~/Desktop/website && git add -A && git commit -m "Re-subset the Chinese font and update the three memos"
  ```

---

## 任务 7：验证

**文件：** 改 scratchpad 的 `verify.js`

> **scratchpad 会被清空。** `verify.js` 和 `node_modules` 丢过一次，
> 重装 `playwright-core` 加重写脚本约十分钟。别当长期存放处。

- [ ] **步骤 1：确认 playwright 还在，不在就装**

  ```bash
  cd /private/tmp/claude-501/-Users-liuyanao-Desktop-website/cda17fe1-99ad-4074-8cfc-c8e102b02263/scratchpad && ls node_modules/playwright-core >/dev/null 2>&1 || npm install playwright-core
  ```

- [ ] **步骤 2：扩充 `verify.js`**

  `SLUGS` **不动**（影评不进「下一组」链条）。新增这些检查：

  - 24 个页面全部 200（原 22 + 影评中英各一）
  - 分类导航三条链接在中英两版都落在**本语言**——
    用 `new URL(href, location.href).pathname` 判，别只看长得对不对
  - 62 个锚点都能 `document.getElementById()` 到
  - 62 张海报 `naturalWidth > 0`（真加载了，不是 404 的破图）
  - 海报墙在 6 个屏幕尺寸下 `scrollWidth <= clientWidth`
  - 中文零缺字形
  - 关掉 JS 渲染结果一致
  - 没有页面引用 3200px 档——**用元素的 `srcset/src` 查，别 grep 正文**，
    照片参数里的「ISO 3200」不是档位

  **判据别写错**（这些每一条都误报过）：大写文字是 CSS `text-transform` 转的、
  首页封面等高只在 >900px 成立、竖屏下限制照片的是宽度不是高度。

- [ ] **步骤 3：本地跑一轮**

  ```bash
  cd /private/tmp/claude-501/-Users-liuyanao-Desktop-website/cda17fe1-99ad-4074-8cfc-c8e102b02263/scratchpad && node verify.js http://localhost:8000
  ```

- [ ] **步骤 4：他 push，然后线上跑一轮**

  ```bash
  cd ~/Desktop/website && git push
  ```

  **线上必须带重试**，否则网络抖动会被误报成网站故障：

  ```js
  async function go(pg,u){for(let i=0;i<5;i++){try{return await pg.goto(u,{waitUntil:'networkidle',timeout:45000});}catch(e){if(i===4)throw e;await pg.waitForTimeout(4000);}}}
  ```

  判断线上是否正常的**唯一标准**是这条命令，不是终端有没有红字：

  ```bash
  curl -s --compressed -o /dev/null -w "%{http_code}\n" https://yanaoliu-jpg.github.io/
  ```

  `200` 就是好的。Actions 报 `deploy: failure` 也可能其实成功了，以 curl 为准。

- [ ] **步骤 5：重量 `docs/` 并回填 容量规划.md**

  ```bash
  cd ~/Desktop/website && python3 - <<'PY'
  from pathlib import Path
  MB = 1048576
  t = lambda p: sum(f.stat().st_size for f in Path(p).rglob('*') if f.is_file())
  d, i, v = t('docs'), t('docs/img'), t('docs/video')
  print(f"docs {d/MB:.0f} MiB  影片 {v/MB:.0f}  照片 {i/MB:.0f}  余 {1024-d/MB:.0f} MiB")
  PY
  ```

---

## 自查（写完计划后对着规格核了一遍）

**规格覆盖：** 一～十节逐条对过，都有任务落点。

| 规格 | 落点 |
|---|---|
| 二、`kind = "notes"` 数据结构 | 任务 2a 步骤 3、任务 3 步骤 1–2 |
| 三、首页分区 + 放弃头题 | 任务 5（步骤 3 专门改 CLAUDE.md） |
| 四、影评页／排序／短评排法／开场白 | 任务 3（步骤 9 定阈值） |
| 五、海报／宽度／片名核对关卡／署名 | 任务 1（关卡）、任务 4 |
| 六、六项内容修订 | 任务 2a 步骤 3 的表 |
| 七、英文版 | 任务 2b |
| 八、字体／verify.js／文档 | 任务 6、任务 7 |
| 十、验收标准 | 任务 7 步骤 2 |

**跟规格的两处偏离（有意的）：**

1. **`tmdb` 编号不写进 `film-notes.toml`**，改放生成的 `posters.toml`。
   理由：手写内容和机器生成的数据分开，`film-notes.toml` 保持纯内容。
   海报靠 slug 关联，不靠编号。
2. **缺海报不中断构建**，只警告。规格没写这一条；按 `build_film` 的路数应该硬失败，
   但 62 张里少 1 张就让他构建不出页面，代价不成比例。

**命名一致性核过：** `build_notes` / `render_notes` / `catnav` / `POSTER_WIDTHS` /
`POSTER_WALL_PX` / `POSTER_ENTRY_PX` / `SHORT_NOTE_CHARS` / `entry["slug"]` /
`entry["poster"]` 在各任务里写法一致。`widths_for` 和 `build_images` 的新参数
都叫 `widths`。

# 影评栏目设计

> 这份规格记的是**这次改动要做成什么样，以及为什么这么定**。
> 日常怎么加作品看 [README.md](README.md)；哪些地方一改就出事看 [CLAUDE.md](CLAUDE.md)；
> 容量看 [容量规划.md](容量规划.md)。
>
> **写于 2026-08-29。实现完成后，这份文件里定下来的规则要并进 CLAUDE.md，这份可以留作存档。**

---

## 一、要做的事

网站从「一面墙」改成**三大类别**，并加入第三类的全部内容：

| 类别 | 英文 | 内容 | 状态 |
|---|---|---|---|
| 照片 | Photographs | 7 组 63 张 | 已有 |
| 影片 | Films | 3 部 | 已有 |
| **影评** | **Film Notes** | **62 部，13,135 字** | **本次新增** |

类别是**可扩展的**——以后加第四类只是多加一个值、多加一区。

源材料：`素材/影评/影评总结_副本.docx`（37 KB，纯文字，无内嵌图片）。

---

## 二、数据结构

### 类别怎么表示

已有的 `kind` 字段扩展成三个值，**不新增字段**：

| `kind` | 含义 |
|---|---|
| 不写 | 照片系列（现状，7 个 toml 都不写） |
| `"film"` | 影片（现状，3 个 toml 在写） |
| `"notes"` | 影评（新增） |

### 影评的文件

**一个文件装 62 部**：`site/content/film-notes.toml`。

不是 62 个文件。理由：影评不是"作品"，是**一页读物**——62 个 toml 会让 `content/` 从 11 个文件涨到 73 个，
而它们全都渲染到同一个页面上。一个文件约 40 KB，可控。

格式（沿用全站 `[zh]` 子表的写法）：

```toml
kind  = "notes"
slug  = "film-notes"
title = "Film Notes"
order = 100                    # 排在所有作品之后

statement = """英文开场白"""

[zh]
title     = "影评"
statement = """中文开场白"""

# ── 一部电影一个 [[entry]]，顺序 = 观影顺序，见第四节 ──
[[entry]]
tmdb  = 1124                   # TMDB 编号，海报靠它取
year  = 2006
title = "The Prestige"
text  = """英文正文"""
  [entry.zh]
  title = "致命魔术"
  text  = """悬疑的成分恰到好处，让人看完后才能顿悟……"""
```

**`[entry.zh]` 紧跟在它的 `[[entry]]` 后面**——TOML 里这指向数组里最后一个元素，顺序错了就串行。

课堂笔记那三篇多写一行 `note_kind = "class"`（见第六节）。

### slug 与锚点

每部电影的 slug 由**英文片名**生成：`The Prestige` → `the-prestige`。
用于锚点 `#the-prestige` 和海报文件名。重名要人工消歧（`blade-runner` / `blade-runner-2049`）。

**slug 一旦发布就不能改**——跟系列页同一条规矩，因为锚点会被引用。

---

## 三、首页的变化

### 新的结构

```
开场白（不变）
分类导航  Photographs · Films · Film Notes     ← 新增
照片      7 组，现有的定高换行墙                  ← 不变，只是加了区标题
影片      3 部，独占一行                        ← 不变，只是加了区标题
影评      62 张小海报密排成一面墙                 ← 新增
页脚（不变）
```

导航只放在 `/`、`/zh/` 和影评页上。**系列页和影片页不加**——那些页面是安静的阅读页，
现有的「全部作品」+「下一组」已经够用，再加一条导航会把注意力从照片上拉走。

### ⚠️ 放弃「停止滑动」的头题位置

CLAUDE.md 第一节写死了：「停止滑动」`order = 0` 排最前当**头题**，理由是
"招生官扫首页第一眼就看到这人还拍片子，而且得奖了"。

**按类别分区之后这件事不可能成立**——影片区必然在照片区之后。

**决定：放弃头题，由分类导航接手。** 顶部那条 `Photographs · Films · Film Notes`
在第一秒里干的是同一件事，而且一次说出三件而不是一件。

「停止滑动」仍然是影片区的第一部（`order = 0` 在影片内部仍然有效）。

> 这一条推翻了 CLAUDE.md 里已经写下的决定，**已经过刘延奥确认**。
> 实现后 CLAUDE.md 第一节相应段落要改掉，不能留着自相矛盾。

### 「下一组」链条不动

现在的链条串起 7 组照片 + 3 部影片共 10 环、首尾相接。**影评不进这条链**——
它是另一种阅读模式，塞进去会让读完照片的人掉进一页一万三千字。链条代码零改动。

---

## 四、影评页

### 网址

`/film-notes/` 和 `/zh/film-notes/`。

用 `film-notes` 不用 `notes`：以后如果有别的笔记，`notes` 这个名字就占死了。
**定了不能改。**

### 版式

一页装完 62 部。每条：

```
小海报 ┃ 片名（英文站显示英文名，中文站显示中文名）· 年份
       ┃ 正文
```

首页那面海报墙的每张图跳到这一页的对应锚点。**影评页不再重复一遍海报墙**——
每条自己带小海报，已经起到索引作用。

### 排序：按观影顺序，也就是原文档的顺序

**不按片名排，不按年份排。**

理由是这一类最有说服力的地方就藏在顺序里：文档从头读到尾，前二十部基本是一句话
（《黑客帝国》"一场视觉盛宴"，6 个字），中间开始拆焦段、机位、布光、调度，
到《拯救大兵瑞恩》已经在写"摄像机与人物的夹角呈 30 度，焦段大概 60-70mm"，
再往后《大都会》《灰熊人》直接是德国表现主义、赛博朋克、巴赞、直接电影的课堂笔记。

**按时间排，这条成长线自己会显出来；按别的排，它就没了。**

### 短评单独一种排法

62 条长度跨度极大：最短 6 字，最长 1566 字，中位数 172 字。

**50 字以内的短评用大一号字、像格言那样排**，不跟长文用同一种版式。
《狮子王》"请记住，不要忘了你自己"、《辩护人》"鸡蛋是活的，石头是死的"——
这些当短句是好的，混在长文里才显得像没写完。

> 50 这个阈值是拍的，实现时**渲染出来看一眼再定**——像照片尺寸当初比过
> 84/74/66/58 四版那样。中文 50 字对应英文大约 30 个词，两版要各看一次。

### 开场白（已定稿）

**中文是刘延奥写的原稿，我只做了语法润色**（「浏览过」→「看过」，长句断开），
英文从中文转：

```
中文：
这里是 2024 年以来，我看过的每一部电影的影评。

里面记着我对电影的执着和热爱，也记着每一次独自看完、独自想过之后新冒出来的发现。
我用文字把它们留了下来——这是我作为影迷的一份宝藏。

英文：
These are my notes on every film I have watched since 2024.

They hold my devotion to cinema and my love for it, and whatever I found new each
time I finished a film alone and thought it through. I have put it all into words —
this is my treasure as a film lover.
```

「执着」译成 `devotion` 不用 `obsession`——后者在英文里偏负面，会读成"沉迷"。

---

## 五、海报

### 来源与流程

1. 从 **TMDB** 取，按 `tmdb` 编号
2. 原图存 `素材/海报/<slug>.jpg`——`素材/` 已在 `.gitignore`，**不进仓库**，跟原图一个待遇
3. 走**现有的图片流水线**（AVIF / WebP / JPEG + srcset），不新增代码路径
4. 产物到 `docs/img/film-notes/`

### 尺寸

海报是 2:3，显示尺寸远小于照片，**用一套单独的宽度：`[200, 400]`**。
墙上每张约 96px 宽，条目里约 120px；400px 覆盖到 2 倍屏还有余量。
不要套用照片的 900/1600/2400——那是给 1400px 宽的照片用的。

体积估算：62 张 × 2 档 × 3 格式，约 **3–6 MB**。相对剩余的 651 MiB 可忽略。

### ⚠️ 下载前必须先核对片名

**片名对错了海报就错了。** 这跟第六组写 alt 文字踩过的坑是同一个：
文件和内容的对应关系必须先确认，不能事后补。

**流程：先把 62 部的「中文名 → 英文名 → TMDB 编号 → 年份」列成表给刘延奥核对，
他确认之后才下载。** 有歧义的（同名重拍、中文译名不唯一）单独标出来问。

### 署名

影评页页脚加一行 `Poster images from TMDB`。TMDB 的条款要求署名，也是诚实的做法。

---

## 六、内容上已经定下的六件事

读完 62 篇后发现的问题。**1、2、4、6 四条是他明确拍板的；3 还没闭合；5 是我说明后他没有异议。**

| # | 问题 | 处理 |
|---|---|---|
| 1 | 《花样年华》正文与《欧维》一字不差（复制粘贴覆盖） | **他已重写**，用新稿 |
| 2 | 《拯救疯人院》片名不存在，正文写的是《飞越疯人院》 | 改成《飞越疯人院》 |
| 3 | 《一个叫欧维的男人的决定去死》末句断在"是如何改" | 片名去掉多余的「的」；**截在前一句收尾**（他授权）：「……而妻子因为车祸而残疾，在事业上也遇到了困难。」 |
| 4 | 《银翼杀手》开头一大段是 IMDb 剧情简介的机翻 | **整段删除**，只留他自己写的「元素」部分 |
| 5 | 全文几十处错别字（「专场」→「转场」3 处、「违一镜到底」→「一镜到底」、「粉币画」→「粉笔画」、「太町市」→「太地町」、「体质」→「体制」、「详略得到」→「详略得当」等） | **只改错字，不动语气和判断** |
| 6 | 《大都会》《灰熊人》《银翼杀手》是短句堆叠的课堂笔记，跟其他 59 篇不是一类 | **原样保留 + 标注「课堂笔记 / Class notes」** |

**六条全部闭合，内容上没有未决项。** 开场白见第四节，也已定稿。

实现中唯一还需要他拍板的是**第五节的片名／TMDB 编号对照表**——那张表必须他确认之后才下载海报。

---

## 七、英文版

**62 篇全部转成英文**，跟全站现在的做法一致（CLAUDE.md 第五节：中文他写、英文我转）。

- 片名用**国际通行英文名**（《致命魔术》→ The Prestige，《大话西游之月光宝盒》→
  A Chinese Odyssey Part One: Pandora's Box）
- 中文版是原稿，英文从它转；**中文不从英文回译**
- 短评译完仍然短，不为了充实而加话

不翻的话，等于把三大类别之一对目标读者关掉——招生官读的是英文站。

---

## 八、会连带变化的东西

### 中文字体会变大（已量过）

| | 现在 | 加影评后 |
|---|---|---|
| 去重汉字 | 834 | **1592**（新增 758） |
| `noto-serif-sc-subset.woff2` | 254 KB | **估计 ~480 KB** |

**这个字体在所有中文页面都加载**，不只影评页。也就是说中文首页会为一个多数人不会打开的页面
多背约 230 KB。

**先照常做，做完量。** 如果中文首页明显变慢，再考虑拆成两个子集
（基础子集 + 影评页专用子集）。**现在不做**——那是 unicode-range 的活，不确定值不值得。

改完中文必须跑：`build.py → subset_font.py → build.py`（顺序不能变）。

### verify.js

- `SLUGS` 数组不动（影评不进「下一组」链条）
- 新增：2 个页面 200、分类导航三条链接落点正确（**中文页必须落在中文页**——
  这条踩过坑，用 `new URL(href, location.href).pathname` 验）
- 新增：62 个锚点都能解析到实际元素
- 新增：海报墙横向溢出为 0（62 张密排，最容易在窄屏溢出）
- 新增：中文零缺字形（新增 758 字，这一项这次尤其重要）
- 新增：关掉 JS 渲染结果一致

### 文档

- **CLAUDE.md**：第一节的「一头两尾」和头题那段要改（见第三节的 ⚠️）；
  加一节讲三大类别和影评的加法
- **README.md**：内容表加影评一行；`content/` 文件表加 `film-notes.toml`
- **容量规划.md**：第二节的占用数字要重算（+3～6 MB 海报，+230 KB 字体）

---

## 九、明确不做的

| 不做 | 为什么 |
|---|---|
| **每部电影一个独立页面** | 62 × 中英 = 124 个新页面（现在全站才 22 个），而中位数只有 172 字——大部分页面会几乎是空的，《黑客帝国》那页就 6 个字 |
| **筛选器 / 搜索 / 分页** | CLAUDE.md 已有的规矩：招生官不会用，还多一层 JS 依赖，跟"关掉 JS 也要能看"冲突 |
| **评分、星级** | 原文里只有两处提到星级（《唐伯虎点秋香》2 星、《致命 ID》4 星），补齐 62 个是在编造 |
| **首页当门厅（三张大卡片）** | 会多一次点击才看得到第一张照片，跟"点开链接第一眼看到作品"直接冲突 |
| **大图海报卡片** | 62 张好莱坞官方海报会盖过他自己的作品——整站每张图都是他拍的，这是它最强的地方 |
| **JS 展开／折叠** | `<details>` 虽然零 JS 可行，但 62 条全折叠等于让人点 62 次 |

---

## 十、验收标准

在现有 585 项的基础上增加。**做完要跑本地和线上两轮**（线上必须带重试，
否则会把网络抖动误报成故障——见 CLAUDE.md 第八节）。

- [ ] 24 个页面全部 200（原 22 + 影评中英各一）
- [ ] 占位符（橙色虚线框）为零
- [ ] 分类导航三条链接在中英两版都落在**本语言**的正确页面
- [ ] 62 个锚点全部能解析到实际元素
- [ ] 62 张海报全部加载成功，无 404
- [ ] 海报墙在 6 个屏幕尺寸下横向溢出均为 0
- [ ] 中文零缺字形（`check_font.py` 全绿）
- [ ] 关掉 JS 后渲染结果与开启时一致
- [ ] 照片墙等高、底边齐等原有检查全部仍然通过（**只在 >900px 查**）
- [ ] 没有页面引用已删除的 3200px 图片档
- [ ] `docs/` 总量重新量过并更新到 容量规划.md

# 容量规划

> 这份纪要回答一个问题：**这个网站还能装多少东西，装满了怎么办。**
>
> 日常怎么加作品看 [README.md](README.md)；哪些地方一改就出事看 [CLAUDE.md](CLAUDE.md)。
> 这里只管容量。**最后核对：2026-08-29。**

---

## 一、三道墙，先认清哪道最近

| 墙 | 谁定的 | 性质 | 超了会怎样 |
|---|---|---|---|
| **单文件 100 MiB** | GitHub | **硬** | `git push` 直接被拒，整次提交推不上去 |
| **站点 1 GB** | GitHub Pages | **硬** | 网站不再发布 |
| 仓库 1 GB | GitHub | 建议值 | 只会收到提醒，不会被拒 |

**最近的一道是单文件，不是总量。** 一部十分钟的 720p 影片，crf26 时 mp4 是 94.6 MiB——
离硬限只剩 5 MiB（后来调到 crf29 才降到 68.7 MiB）。总量那边却还有一大半没用。

还有一条容易忽略的：**`.git` 只增不减**。删掉文件只会让*发布的站点*变小，
仓库反而更大——git 永远保留历史里的每一个字节。2026-08 删掉 3200px 档时，
`docs/` 少了 110 MiB，`.git` 却涨了。

---

## 二、当前占用（2026-08-29 实测）

```
发布的站点 docs/          373 MiB  / 1024 MiB 上限      余 651 MiB
  ├─ 影片                 258 MiB   (69%)
  ├─ 照片                 114 MiB   (31%)
  └─ 网页/字体/CSS/JS       0.9 MiB

仓库 .git                 558 MiB  （只增不减）
```

**单文件排行**（100 MiB 是硬限）：

| 文件 | 大小 | 占硬限 |
|---|---|---|
| `make-a-wish.webm` | 83.7 MiB | **84%** |
| `make-a-wish.mp4` | 68.7 MiB | 69% |
| `gratitude.mp4` | 43.9 MiB | 44% |
| `gratitude.webm` | 40.7 MiB | 41% |
| `stop-scrolling.mp4` | 14.5 MiB | 15% |
| `stop-scrolling.webm` | 6.3 MiB | 6% |

⚠️ 现在**离硬限最近的是 webm 那条（84%）**，不是 mp4。
下一部再长的片子，`av1_crf` 也得跟着往上调，不能只盯着 h264。

---

## 三、实测速率（用来估算，不要拍脑袋）

| 内容 | 速率 |
|---|---|
| 一组照片（9 张） | 约 **16 MiB**（去掉 3200 档和 2400 JPEG 之后） |
| 720p 影片 mp4（crf 26） | 约 **9.3 MiB／分钟** |
| 720p 影片 mp4（crf 29） | 约 **6.5 MiB／分钟** |
| 720p 影片 webm（AV1 crf 36） | 约 **8.2 MiB／分钟** |

**估一部片子会不会撞墙**：

```
时长(分钟) × 9.3 ≈ mp4 的 MiB 数（720p crf26）
时长(分钟) × 6.5 ≈ mp4 的 MiB 数（720p crf29）
```

→ crf26 的上限是 **10 分 45 秒**，crf29 是 **15 分 20 秒**。

---

## 四、已经用掉的牌

这两张打完就没了，别指望再打第二次。

**① 去掉 3200px 图片档**（2026-08）
省 110 MiB，占图片体积 41%。代价：16 吋 MacBook Pro 上最宽的几张照片
需要 2800px 却只拿到 2400px，差 14%——那是放大到 1400px 宽时的密度差，
肉眼基本看不出。
*要撤销*：把 `3200` 写回 `build.py` 的 `WIDTHS`，跑一次增量构建自动补出来。

**② JPEG 兜底档从 2400 降到 1600**（2026-08）
省 45 MiB（实测 `docs/` 从 418 → 373 MiB）。JPEG 只服务于既不支持 AVIF 也不支持 WebP 的浏览器
（Safari 13 以下、IE），那些机器的屏幕本来也没那么大。
现代浏览器走 AVIF/WebP，那两种格式仍然出到 2400px。**几乎零代价。**
*要撤销*：`JPEG_MAX_WIDTH` 改回 2400。

---

## 五、还剩的牌，按推荐顺序

### 🥇 影片改用对象存储（Cloudflare R2）

把 `<source src>` 从 `/video/...` 换成 `https://xxx.r2.dev/...`。

- **优点**：影片完全移出 1 GB 预算（立刻腾出 256 MiB，以后每部片子都不占），
  单文件 100 MiB 的限制也一起消失。**而且仍然是纯 `<video>` 标签——
  零 JavaScript、零追踪**，不违反"关掉 JS 也要能看"那条原则。
  R2 免费额度 10 GB 存储 + 不收流量费，这个作品集的量远够。
- **缺点**：给一个"要自己维护三年、零外部依赖"的项目引入了一个外部服务。
  那个域名一旦失效，所有影片同时挂掉，而网申表格里填的链接是主站——
  招生官会看到一个能打开但影片全黑的网站。
- **什么时候上**：真的要放第四、五部长片时。现在不用。
- **怎么做**：`build.py` 里给影片的 URL 加一个前缀变量，本地构建仍然写
  `docs/video/`（方便预览），发布时改成 R2 的地址。`.gitignore` 掉 `docs/video/`。

### 🥈 降低影片码率

把 `h264_crf` 从 26 调到 29，`av1_crf` 从 36 调到 40。省约三成。

- **优点**：一行 toml，立刻见效，不引入任何依赖。
- **缺点**：全屏看会软一点。访谈片影响小，有画面细节的片子影响大。
- **⚠️ 坑**：改了 toml **不会自动重转**——`is_fresh` 只比对源文件的时间。
  必须先删掉 `docs/video/<slug>/` 里的产物，或者用 `--force`（但那会把
  63 张照片全部重编，十几分钟）。

### 🥉 影片降到 540p

省约 45%。

- **优点**：省得最多。
- **缺点**：电脑全屏看明显发糊。**一部十分钟的片子不值得这么牺牲**，
  除非它本来就是手机竖屏或画质本来就一般。

### 精选：删掉一些作品

- **优点**：免费，而且**会让作品集更好**。招生官在一份作品集上只停留几分钟，
  10 组照片已经要滚 4.6 屏；第 11 组开始每加一组都在稀释前面的。
- **缺点**：没有。这条其实应该排第一，只是它不算"技术方案"。

---

## 六、不要做的（附原因）

| 方案 | 为什么不 |
|---|---|
| **Git LFS** | **GitHub Pages 不解析 LFS**——它会把指针文本文件当成视频发出去，网站直接坏掉。这是个陷阱，看着像标准答案，实际不能用。 |
| **去掉 2400px 图片档** | 能省 99 MiB，但视网膜笔记本上照片会**明显发软**。这是唯一一刀真正砍到作品本身。 |
| **降低 AVIF 质量** | q72 是当初拿这批夜景照片实测暗部表现选出来的（AVIF q72 的暗部误差优于 JPEG q90）。省不了多少，赌注却是照片最要命的那部分。 |
| **YouTube / Vimeo 嵌入** | 外部 JavaScript + 隐私追踪 + 播放器上的品牌和推荐视频。R2 那条路能达到同样的省空间效果，且没有这些问题。 |
| **分仓库** | 影片单开一个 repo + Pages 站，各自 1 GB。可行，但两套发布流程、跨域 URL、复杂度翻倍，收益不如 R2。 |

---

## 七、决策速查

**要加一部影片？** 先算：`时长(分钟) × 9.3` MiB。

- **< 60 MiB** → 直接加，现参数（720p / crf26 / av1 36）
- **60–95 MiB** → 能加，但会贴近单文件硬限。**把 `h264_crf` 调到 29**
- **> 95 MiB** → 撞墙。要么 crf 29（上限推到 15 分 20 秒），要么上 R2

**要加一组照片？** 约 16 MiB，随便加——照片不是容量问题，
**注意力才是**：别超过 10–12 组。

**docs/ 超过 800 MiB 了？** 到了该上 R2 的时候。

---

## 八、这份纪要什么时候要重算

改动下面任何一样，上面的数字就作废了，重新量一遍：

- `build.py` 的 `WIDTHS` / `JPEG_MAX_WIDTH` / `AVIF_QUALITY`
- 任何影片的 `height` / `av1_crf` / `h264_crf`
- 加了新影片

重新量的命令：

```bash
cd ~/Desktop/website && python3 - <<'PY'
from pathlib import Path
MB = 1048576
t = lambda p: sum(f.stat().st_size for f in Path(p).rglob('*') if f.is_file())
d, i, v = t('docs'), t('docs/img'), t('docs/video')
print(f"docs {d/MB:.0f} MiB  影片 {v/MB:.0f}  照片 {i/MB:.0f}  余 {1024-d/MB:.0f} MiB")
for f in sorted(Path('docs/video').rglob('*.*')):
    print(f"  {f.name:28s} {f.stat().st_size/MB:6.1f} MiB = 硬限的 {100*f.stat().st_size/(100*MB):3.0f}%")
PY
```

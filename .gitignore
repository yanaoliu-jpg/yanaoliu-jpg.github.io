# ── 原图：11 GB，绝对不能进仓库 ────────────────────────────────
# 原图只留在你自己电脑上。仓库里只有压缩后的成品（docs/）。
# 这也意味着：换电脑之前，素材/ 要自己另外备份，git 不会帮你备份它。
素材/

# ── 之前那次没做完的 Hugo 主题尝试 ─────────────────────────────
# 它自带 .git，混进来会变成 git 子模块，很麻烦。现在也用不上了。
bridget/

# ── 构建过程的临时文件 ────────────────────────────────────────
site/_work/
site/dist/

# ── macOS 和编辑器 ────────────────────────────────────────────
.DS_Store
.AppleDouble
._*
.vscode/
.idea/
*.swp

# ── Python ───────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/

# 首页改版设计

> 这份规格记的是 **2026-09 首页改版要做成什么样，以及为什么这么定**。
> 日常怎么加作品看 [README.md](README.md)；哪些地方一改就出事看 [CLAUDE.md](CLAUDE.md)。
>
> **写于 2026-09-21。实现完成后，这份文件里定下来的规则要并进 CLAUDE.md，这份可以留作存档。**
> 决策全部经刘延奥在样稿上点选确认，样稿留在 `.superpowers/brainstorm/`（不进仓库）。

---

## 一、起因

他的原话：「封面的页面设计太平了 不够吸引人不够醒目 不要是黑色底 颜色可以多元丰富一些」。
后来又加了两条：换一个跟新风格搭的字体；封面小一点，一页装更多、少滚动。

## 二、深色底的前提已经不成立了（数据）

CLAUDE.md 第四节写着「这组照片 91% 的像素亮度在 32 以下，所以整站深色」。
那是**只有第一组时**量的。2026-09-21 把十张封面重新量了一遍：

| 作品 | 平均亮度 | 亮度 < 32 的像素 |
|---|---|---|
| Good Night（第一组） | 27 | **72%** |
| Looking Forward | 41 | 50% |
| I Keep Going Back | 57 | 47% |
| Sea and Light | 54 | 45% |
| Drunk on the Night Wind | 58 | 33% |
| Make a Wish（影片） | 115 | 13% |
| Stop Scrolling（影片） | 87 | 8% |
| Gratitude（影片） | 137 | 6% |
| Goodbye, Renfen | 90 | 5% |
| The Old Days | 104 | 4% |
| **十张平均** | **77** | **28%** |

十张里只有第一组还是真的黑。五张中等亮度的灰调封面放在 `#0d0e11` 上，
既不是暗对暗的高级，也不是亮对暗的醒目，是**灰对黑的浑**——他说的「平」就是这个。

**但这条前提在系列页上仍然成立。** 点进 Good Night 看全尺寸时，72% 的像素是暗部，
白底会让眼睛适应白色、暗部细节直接看没。那是招生官看作品本身的地方，不拿来冒险。

→ **首页换亮底，内页保持深色。** 画廊就是这么做的：白墙挂画，放映厅关灯。

## 三、六个决定（全部在样稿上点选过）

| # | 问题 | 定了 | 没选的 |
|---|---|---|---|
| 1 | 改哪些页面 | **只有首页换底**（`/` 和 `/zh/`） | 首页+影评页；全站 |
| 2 | 颜色方向 | **氛围渐变**：一整片低饱和的多色渐变，封面带影浮在上面 | 纸色底+每张封面垫照片自己的色板；三区三色的大色块 |
| 3 | 封面边缘 | **圆角 10px + 柔影** | 白边卡纸；小圆角+细描边；现状直角 |
| 4 | 第一屏 | **保持现状**：名字占满第一屏，作品在第二屏 | 紧凑（第一排封面进第一屏）；最新一组当主图 |
| 5 | 字体 | **DM Sans + Noto Sans SC，全站换** | Fraunces；Instrument Serif + DM Sans；保留 Newsreader |
| 6 | 封面大小 | **1440 上 300px**（原 400），**影片跟照片并排**（原每部独占一行） | 240px；保留 400px |

第 3 条他是在我建议「白边卡纸或细描边」之后选的圆角——照做，不再劝。

第 5 条终端里打的是「1」（Fraunces），浏览器记到的点击是「2」（DM Sans），
追问后确认是 **DM Sans**。以后终端和点击对不上的，一律追问，不猜。

## 四、怎么做：一套 CSS，首页亮、内页暗

**不写第二份样式表。** `base.html` 的 `<html>` 加一个 class 槽，
只有 `write_index` 渲染的两个页面得到 `theme-home`：

```html
<html lang="…" class="no-js theme-home">     ← 只有 / 和 /zh/
<html lang="…" class="no-js">                ← 其余 22 个页面，什么都不加
```

```css
:root { --bg: #0d0e11; --ink: #e9e5dd; … }          /* 深色，内页照旧 */
.theme-home { --bg: …; --ink: …; --ink-dim: …; … }   /* 同一批变量换值 */
```

所有组件读的还是同一批变量，所以内页一行不改。`<meta name="theme-color">` 和
`color-scheme` 也按页给——首页 `theme-color` 用渐变最上面那一站 `#f3e4dc`、`color-scheme: light`；
内页照旧 `#0d0e11` / `dark`。不这么做的话手机地址栏在亮首页上还是黑的。

### 渐变

```css
.theme-home body {
  background: linear-gradient(160deg, #f3e4dc 0%, #e7ece4 28%, #d7e9e6 55%, #dde3ee 80%, #efe6dc 100%);
}
```

五个色站**从封面的色板里来**：玫瑰是「晚安」窗光、青是「海与光」、黄绿是「留给日后」、
蓝是「感恩」。方向 A「颜色从照片来」那条思路，在 C 里以这种方式活着。
渐变铺在整个文档上（body 有多高就多高），不固定在视口——页面越长过渡越缓，各区自然落在不同的色调里。

具体色值实现时**渲染出来微调**，但必须满足第六节的对比度底线。

## 五、封面

- `.work__frame { border-radius: 10px; overflow: hidden; }` + 两层影
  （一层散：`0 22px 44px -22px rgba(20,30,40,.5)`；一层贴边：`0 2px 6px rgba(20,30,40,.08)`）。
  LQIP 模糊底图在同一个元素上，跟着裁圆角。
- **`--cover-h`：1440 上 300px。** 更窄的屏等比缩，下限渲染 1024 / 1280 定。
  `build.py` 的 `INDEX_COVER_H` **必须同步**——CLAUDE.md 第三节第 2 条那种「两处必须一致」。
- **影片并排**：去掉 `.work--film { flex: 0 0 100% }` 和它的 `max-width` 覆盖。
  16:9 在 300 高就是 533 宽，跟照片一样按宽高比占位。三部影片仍然在自己的 `#films` 区里。
- ▶ 角标改成**白底深字**（原来是给深底设计的半透明白）。
- 首页海报墙：4px 圆角；去掉 `brightness(.86)`——那是深底上压亮度用的，亮底上会发浑；`saturate(.9)` 留着。
- 影评页自己的条目海报**不动**（那页是深色的）。

### 为什么影片并排是「少滚动」的大头

1440×900 上实测，改版前整页 7.7 屏：

| 区 | 屏数 | 内容 |
|---|---|---|
| 开头 | 1.57 | 名字 + 开场白 |
| 照片 | 2.03 | 7 组，3 行 |
| **影片** | **2.28** | **3 部，每部独占一行** |
| 影评 | 1.10 | 62 张小海报 |

3 部影片比 7 组照片占的还多。封面从 400 缩到 300 省不到 1 屏，影片并排省 1.5 屏。

**实测（实现后）：** 照片 + 影片从 4.31 屏降到 2.77 屏（−1.54，比样稿估的还多），
但**整页是 6.05 屏，不是这里第一版写的「约 5 屏」**——样稿的估算漏了区间距和说明文字。
剩下的大头是开头那 1.6 屏（决定 4 保留的）。他选了「区间距按封面比例收 25%」，
没有回头动开头。要再往下只能压开头，那是另一个决定。

## 六、字体：DM Sans + Noto Sans SC，全站

| | 原来 | 现在 |
|---|---|---|
| 拉丁 | Newsreader（编辑感衬线） | **DM Sans**（人文无衬线，变量字体：字重 300–600 + 光学尺寸轴 9–40） |
| 中文 | Noto Serif SC 子集 | **Noto Sans SC 子集** |

**为什么全站而不只首页**（他选的是「全站」）：
1. 字体是身份，不是背景。首页无衬线、点进去衬线，比换底色刺眼得多。
2. 只换首页的话，中文站要同时加载两套中文子集（Serif 527 KB + Sans 约 500 KB），白背一倍。
3. 影评页一万三千字正文，正文向的无衬线在那儿收益最大。

**字体流水线只改两处：**
- `site/static/fonts/newsreader-latin.woff2` → `dm-sans-latin.woff2`（自托管拉丁子集，来自 fonts.gstatic.com，跟当初 Newsreader 同一来源）
- `subset_font.py` 里完整字体的下载地址从 `NotoSerifSC[wght].ttf` 换成 `NotoSansSC[wght].ttf`，
  缓存文件名跟着改，重裁一次。本地裁的机制、先验后覆盖的保护，一行不动。

两个下载**他已批准**（2026-09-21）。两个字体都是 OFL 许可，自托管合规。

字重：现在 h1 用 300、正文 400、最重不超过 600，DM Sans 全有。
`font-optical-sizing: auto`（默认）让光学尺寸轴自己按字号切，大标题和 10px 的说明各用各的形。

## 七、亮底上的对比度要重算

现在的 `--ink-dim / --ink-mute / --ink-faint` 是在 `#0d0e11` 上过 4.5:1 的，搬到浅底上全作废。

**规则：三档灰全部对着渐变**最深**的那一站算，底线 4.5:1（WCAG AA 小字）。**
`verify.js` 加一项：读计算样式，直接算对比度，不靠肉眼。

> ⚠️ 这一段第一版写的是「对最浅的一站算」，**方向反了**，实现时发现的。
> 深色字在浅底上，底越深对比度越低，所以卡脖子的是最深的一站 `#dde3ee`（亮度 0.765），
> 不是最浅的 `#e7ece4`（0.826）。五站都算过，最差的 `--ink-faint #64625c` 在最深站上 4.73:1。

**琥珀色 `#d9a05b` 在浅底上做文字过不了 4.5:1**（对 `#f3e4dc` 算出来是 **1.9:1**）。
它在首页**只能当线用**——导航当前项的下划线、分隔——不能当字。内页照旧。

## 八、不变的

- 第一屏的比例和内容（决定 4）
- 三大类别、导航、`order`、「下一组」链条、编号
- 内页（系列 / 影片 / 影评）的深色底和版式
- 图片流水线、`sizes` 规则、AVIF 保险丝
- `.js-reveal` 纪律：所有 `opacity: 0` 仍然挂在它下面，`gallery.js` 的选择器不变
- **所有网址**

## 九、验收

在现有 320 项之上新增，本地和线上各跑一轮：

- [ ] `html.theme-home` **只**出现在 `/` 和 `/zh/`，其余 22 页没有
- [x] 首页三档灰对渐变**最深**色站 `#dde3ee` 的对比度 ≥ 4.5:1（脚本算，不靠看）
- [x] 1440 上 `#photographs` 的封面高 300px，且仍然等高（>900px 才查）
- [x] 影片封面宽 ≈ 1.78 × 300 ≈ 533px，**不再撑满一行**
- [x] **整页 ≤ 6.1 屏**（1440×900）——「滚动太多」的回归防线。
      第一版写的 5.5 是估错的（见第五节），实测 6.05，防线定在它上面一点
- [ ] `document.fonts.check('16px "DM Sans"')` 在全部 24 页为真
- [ ] Noto Sans SC 零缺字（`check_font.py`）
- [ ] 首页 `theme-color` 是 `#f3e4dc`、`color-scheme` 是 `light`；内页仍是 `#0d0e11` / `dark`
- [ ] 关掉 JS 渲染一致；无横向溢出；无 3200 档引用；原有检查全过

## 十、文档

- **CLAUDE.md 第四节「深色底」那段重写**：91% 只对第一组成立，换成第二节那张表；
  说清「首页亮、内页暗」和为什么。第三节第 2 条加 `INDEX_COVER_H ↔ --cover-h` 的新值。
  第二节结构里字体文件名。第八节验证加新项。
- **README**「为什么整站是深色的」→ 改成「为什么首页是亮的、内页是深的」；字体一句。
- **容量规划** 第二节字体的数字。
